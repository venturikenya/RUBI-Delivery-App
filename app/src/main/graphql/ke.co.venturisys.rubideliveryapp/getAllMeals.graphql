# Here we get all the meals saved in database
query allMealsQuery{
  allFoods {
    edges{
      node{
        foodName
        foodDescription
        quantityAvailable
        belongsTo {
          categoryName
        }
        foodImage
        unitValue
      }
    }
  }
}